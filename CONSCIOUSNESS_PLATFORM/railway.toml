[build]
builder = "nixpacks"

[deploy]
startCommand = "python backend/platform_api.py"
healthcheckPath = "/health"
healthcheckTimeout = 100
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 10
